import { Lobster, Quicksand, Comfortaa } from 'next/font/google';

export const lobster = Lobster({ weight: ['400'], subsets: ['latin'] });

export const quicksand = Quicksand({ weight: ['700'], subsets: ['latin'] });

export const comfortaa = Comfortaa({ weight: ['700'], subsets: ['latin'] });
